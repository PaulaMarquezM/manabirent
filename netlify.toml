[build]
  command = "npm run build"
  publish = "dist"

# React Router resuelve las rutas en el cliente. Sin esta regla, abrir o
# recargar una URL como /inmueble/:id devolvería un 404 en Netlify.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
